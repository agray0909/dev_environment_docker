ide:
  image: gray/dev:v8
  privileged: true
  volumes:
    - /tmp/.X11-unix:/tmp/.X11-unix:ro
    - /dev/bus/usb:/dev/bus/usb
    - ~/dev:/home/gray/dev:Z
  environment:
    - DISPLAY
  
chide:
  image: ch/dev:v1
  volumes:
    - /tmp/.X11-unix:/tmp/.X11-unix:ro
  environment:
    - DISPLAY
